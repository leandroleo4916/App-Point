<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    android:orientation="vertical">

    <Spinner
        android:id="@+id/spinnerGetEmployee"
        android:layout_width="300dp"
        android:layout_height="50dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:ignore="MissingConstraints"
        tools:layout_editor_absoluteY="0dp" />

    <TextView
        android:id="@+id/datePoint"
        android:layout_width="90dp"
        android:layout_height="30dp"
        android:text="12 Set"
        android:textAlignment="center"
        android:textColor="@android:color/white"
        app:layout_constraintEnd_toEndOf="@+id/spinnerGetEmployee"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toEndOf="@+id/hourPoint"
        app:layout_constraintTop_toBottomOf="@+id/spinnerGetEmployee"
        tools:ignore="MissingConstraints" />

    <TextClock
        android:id="@+id/hourPoint"
        android:layout_width="80dp"
        android:layout_height="30dp"
        android:layout_marginStart="0dp"
        android:text="22:00"
        android:textAlignment="center"
        android:textColor="@android:color/white"
        app:layout_constraintBottom_toBottomOf="@+id/datePoint"
        app:layout_constraintStart_toStartOf="@+id/spinnerGetEmployee"
        app:layout_constraintTop_toBottomOf="@+id/spinnerGetEmployee"
        app:layout_constraintVertical_bias="0.0"
        tools:ignore="MissingConstraints" />


</androidx.constraintlayout.widget.ConstraintLayout>
